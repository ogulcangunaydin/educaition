# For Development: cp .env.example .env
# For Production:  Set these as environment variables on server

# Environment: development, staging, or production
# - development: Debug mode ON, relaxed CORS, seed users allowed
# - staging: Debug mode OFF, restricted CORS, seed users allowed  
# - production: Debug mode OFF, strict validation, no seed users
APP_ENV=development

# Debug mode (auto-enabled in development if not set)
# DEBUG=true

# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# MUST be at least 32 characters in production!
SECRET_KEY=your-secret-key-at-least-32-characters-long

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

DATABASE_URL=postgresql://user:password@localhost:5432/educaition

# OpenAI for AI features
# OPENAI_API_KEY=your-openai-api-key
